{
  "name": "sandbox",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "react-scripts": "1.0.10"
  },
  "dependencies": {
    "jquery": "^3.2.1",
    "npm-run-all": "^4.0.2",
    "react": "^15.6.1",
    "react-dom": "^15.6.1",
    "stylus": "^0.54.5"
  },
  "scripts": {
    "stylus": "stylus -w src/ -o src/",
    "start-js": "react-scripts start",
    "start": "npm-run-all -p stylus start-js",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
